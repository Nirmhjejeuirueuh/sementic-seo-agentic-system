# Formats — not populated yet

This taxonomy dimension (Bust, Full-body standing, Keychain, Urn, Cake
Topper, Centerpiece…) is scaffolded but empty in the first pass. Several
products in the source document reference a format, but none of the 5
starter products in this pass need one resolved.

Add `formats/*.md` notes (same convention as `occasions/`, `types/`,
`styles/`) when scaling past the first 5 products. See
`data/vault/README.md`.
